<Window
    x:Class="ABX.Hub.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
    Title="ABX – Hub"
    Width="1280"
    Height="800"
    Background="#FFF7F9FB"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">

    <!--
        Bố cục:
        - Lớp dưới: WebView2 (tải hub.html).
        - Lớp trên: HostContainer (ẩn/hiện khi vào game), chứa ContentControl để nhét view plugin.
        Các tên x:Name phải trùng với code-behind: web, HostContainer, HostContent.
    -->
    <Grid>

        <!--  WebView2 hiển thị hub.html  -->
        <wv2:WebView2
            x:Name="web"
            Margin="0"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            Grid.ZIndex="1" />

        <!--  Vùng hiển thị plugin, ẩn mặc định  -->
        <Grid
            x:Name="HostContainer"
            Background="{x:Null}"
            Grid.ZIndex="2"
            Visibility="Collapsed">
            <!--  Bạn có thể bọc ContentControl bằng Border/ScrollViewer nếu cần  -->
            <ContentControl
                x:Name="HostContent"
                Margin="0"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch" />
        </Grid>

    </Grid>
</Window>
